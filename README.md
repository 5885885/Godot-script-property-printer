A node that can be dropped into a prexisting scene and be assigned a node to print properties for.
Prints every time the timer times out. 
For when using _process() is excessive but you want regular updates.
